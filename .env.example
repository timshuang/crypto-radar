# =============================================================================
# Crypto Radar - 环境变量配置模板
# =============================================================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 部署后访问配置页面填写敏感信息
# 3. 确保 .env 文件不被提交到 Git（已在 .envignore 中排除）
# 
# 敏感数据说明：
# - 密码（API_TOKEN）已迁移至 config.json
# - BARK_KEY, TG_BOT_TOKEN, TG_CHAT_ID 只存储在 .env 文件
# - config.json 不存储除密码外的其他敏感数据
# =============================================================================

# =============================================================================
# Bark 通知配置（敏感）
# -----------------------------------------------------------------------------
# 获取方式：在 iPhone 上安装 Bark App，打开后复制设备 Key
# 文档：https://apps.apple.com/app/id1403753865
# 部署时留空，启动后在配置页面填写
BARK_KEY=

# 铃声名称（可选，默认：minuet）
BARK_SOUND_NORMAL=minuet
BARK_SOUND_CRITICAL=alarm

# 音量（1-10，默认：5，仅在紧急模式生效）
BARK_VOLUME=5

# -----------------------------------------------------------------------------
# Telegram 通知配置（敏感）
# -----------------------------------------------------------------------------
# 获取方式：
# 1. 联系 @BotFather 创建机器人，获取 Token
# 2. 联系 @userinfobot 获取你的 Chat ID
# 部署时留空，启动后在配置页面填写
TG_BOT_TOKEN=
TG_CHAT_ID=

# -----------------------------------------------------------------------------
# 测试模式
# -----------------------------------------------------------------------------
# true  = 仅打印 URL，不实际发送通知（用于调试）
# false = 正常发送通知（生产环境）
NOTIFY_TEST_MODE=false
