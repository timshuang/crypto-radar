# 快速测试指南

## 🚀 启动服务

```bash
cd /root/.openclaw/workspace/xia-zhihui/projects/crypto_radar
node src/index.js
```

## 🌐 访问界面

打开浏览器访问：`http://localhost:3000`

## 📱 测试流程

### 1. 查看新 UI
- 打开仪表盘页面
- 看到顶部紫色渐变的"系统监控"卡片
- 开关显示为绿色（开启状态）
- 状态文本显示"系统运行中"

### 2. 测试关闭流程
1. 点击开关滑块 → 开关变灰
2. 弹出确认对话框（带⚠️图标）
3. 点击"取消" → 弹窗关闭，开关恢复绿色
4. 再次点击开关滑块 → 弹出确认对话框
5. 点击"确认关闭" → 系统停止
6. 看到右上角 Toast 提示："系统已停止监控"
7. 状态文本变为"系统已停止"

### 3. 测试开启流程
1. 点击开关滑块（直接开启，无需确认）
2. 看到右上角 Toast 提示："系统已启动"
3. 状态文本变为"系统运行中"

### 4. 测试 API（可选）

```bash
# 测试 API
node test-system-toggle.js
```

## 🎨 UI 预览

### 开关卡片
```
┌─────────────────────────────────────┐
│  🔌 系统监控              [开关]    │
│  系统运行中                         │
└─────────────────────────────────────┘
```

### 确认弹窗
```
┌──────────────────────────────────┐
│              ⚠️                   │
│   确认关闭系统监控？              │
│                                  │
│  关闭后将不再监控任何币种，       │
│  不会接收价格更新和告警推送。     │
│                                  │
│     [取消]    [确认关闭]         │
└──────────────────────────────────┘
```

## ✅ 验收标准

- [x] 开关样式美观，类似币种管理页面
- [x] 关闭时有确认弹窗保护
- [x] 开启时直接切换，无需确认
- [x] 状态文本实时更新
- [x] Toast 提示操作结果
- [x] 错误处理完善
- [x] UI 响应式设计

## 🐛 故障排查

### 开关没反应
- 检查浏览器控制台是否有错误
- 确认服务器已启动
- 检查网络连接

### 样式不显示
- 清除浏览器缓存
- 强制刷新：Ctrl+Shift+R (Windows) / Cmd+Shift+R (Mac)

### API 调用失败
- 检查 API Token 是否正确
- 确认后端服务正常运行
- 查看服务器日志
